{% extends 'base.html.twig' %}

{% block title %}{{'New user'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Create new User'|trans}}</h1>

    {{ include('user/_form.html.twig') }}

    <a class="btn btn-primary mt-3" href="{{ path('user_index') }}">{{'back to list'|trans}}</a>
{% endblock %}
