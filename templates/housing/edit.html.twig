{% extends 'base.html.twig' %}

{% block title %}{{'Edit Housing'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Edit Housing'|trans}}</h1>

    {{ include('housing/_form.html.twig', {'button_label': 'Update'}) }}

<div class="text-right">
    <a class="btn btn-primary" href="{{ path('housing_index') }}">{{'back to list'|trans}}</a>
</div>
    {{ include('housing/_delete_form.html.twig') }}
{% endblock %}



